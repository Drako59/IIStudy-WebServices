@using LLStudy_Models.ViewModels;
@using LLStudy_Models.Models;

@model ViewRegisterdBookCatalogModel;

@{
    ViewData["Title"] = "ViewBookCatalog";
    Layout = "~/Views/Shared/RegisteredMasterPage.cshtml";

}


<link rel="stylesheet" href="../Css/bookCatalog_claude.css">

<section class="search-section">
    <div class="container">

        <div class="catalog-toolbar">

            <!-- כפתור שלושת הקווים -->
            <button type="button"
                    class="filters-menu-button"
                    onclick="openFiltersSidebar()"
                    aria-label="Open filters">

                <span></span>
                <span></span>
                <span></span>

            </button>

            <!-- החיפוש הקיים -->
            <div class="search-container">
                <input type="text"
                       id="search-input"
                       class="search-input"
                       placeholder="Search for books, authors, categories..."
                       oninput="searchBooks()">

                <div class="search-icon">🔍</div>
            </div>

        </div>

    </div>
</section>

<!-- רקע כהה מאחורי ה-Sidebar -->
<div id="filters-overlay"
     class="filters-overlay"
     onclick="closeFiltersSidebar()">
</div>


<!-- Sidebar Filters -->
<aside id="filters-sidebar"
       class="filters-sidebar">

    <div class="filters-sidebar-header">

        <div>
            <h2>Book Filters</h2>
            <p>Choose the books you want to display</p>
        </div>

        <button type="button"
                class="close-filters-button"
                onclick="closeFiltersSidebar()">
            ×
        </button>

    </div>


    <form method="get"
          action="@Url.Action("ViewBookCatalog", "Registered")"
          class="filters-form">

        <!-- Subject -->
        <div class="filter-group">

            <label for="subjectID">
                Subject
            </label>

            <select id="subjectID"
                    name="subjectID"
                    class="filter-select">

                <option value="">
                    All Subjects
                </option>

                

            </select>

        </div>


        <!-- Price -->
        <div class="filter-group">

            <label>
                Price Range
            </label>

            <div class="price-inputs">

                <div class="price-input-wrapper">
                    <span>$</span>

                    <input type="number"
                           name="minPrice"
                           placeholder="Min"
                           min="0"
                           step="0.01">
                </div>

                <span class="price-divider">—</span>

                <div class="price-input-wrapper">
                    <span>$</span>

                    <input type="number"
                           name="maxPrice"
                           placeholder="Max"
                           min="0"
                           step="0.01">
                </div>

            </div>

        </div>


        <!-- In Stock -->
        <div class="filter-group">

            <label class="filter-option">

                <input type="checkbox"
                       name="inStockOnly"
                       value="true">

                <span class="filter-option-checkbox">
                    ✓
                </span>

                <span class="filter-option-information">
                    <strong>In Stock</strong>
                    <small>Show books currently in stock</small>
                </span>

                <span class="filter-option-dot stock-dot"></span>

            </label>

        </div>


        <!-- Online Books -->
        <div class="filter-group">

            <label class="filter-option">

                <input type="checkbox"
                       name="onlineOnly"
                       value="true">

                <span class="filter-option-checkbox">
                    ✓
                </span>

                <span class="filter-option-information">
                    <strong>Online Books</strong>
                    <small>Show online books only</small>
                </span>

                <span class="filter-option-dot online-dot"></span>

            </label>

        </div>


        <div class="filters-form-buttons">

            <!-- מאפס את הפילטרים באמצעות מעבר לעמוד בלי Query String -->
            <a href="@Url.Action("ViewBookCatalog", "Registered")"
               class="clear-filters-button">
                Clear
            </a>

            <!-- שולח את הטופס לשרת -->
            <button type="submit"
                    class="apply-filters-button">
                Apply Filters
            </button>

        </div>

    </form>

</aside>

<!-- Main Content -->
<main class="main-content">

    <div class="container">
        <div class="books-grid">
           





            @if (Model != null && Model.books != null && Model.books.Any())
            {
                @foreach (Book book in Model.books)
                {
                    if (book.In_stock)
                    {
                        bool isPurchased = Model.OwnedOnlineBooksIDs != null && Model.OwnedOnlineBooksIDs.Contains(book.BookID);
                        bool isInCart = Model.OnlineBooksInShoppingCartIDs != null && Model.OnlineBooksInShoppingCartIDs.Contains(book.BookID);
                        string path = $"{@Url.Action("ViewBookPreview", "Registered", new { bookID = book.BookID })}";
                        
                        <div class="book-card" data-title="@book.Book_name">
                            <a href=@path class="book-cover">
                                @if (!string.IsNullOrWhiteSpace(book.SubjectID))
                                {
                                    <span class="book-subject-tag">
                                        @book.SubjectID
                                    </span>
                                }
                                <img class="image_div" src="@Url.Action("GetBookImage", "Guest", new { bookID = book.BookID })">
                            </a>
                            <div class="book-info">
                                <h3>@book.Book_name</h3>
                                <p class="book-author">@book.Author_name</p>
                                <p class="book-price">$@book.Book_price</p>
                                <button class="add-to-cart @(Model.OwnedOnlineBooksIDs.Contains(book.BookID) ? "purchased" : isInCart ? "in-cart" : "")" id="book-@book.BookID"
                                        @((isPurchased || isInCart) ? "disabled" : "")
                                        onclick="@((isPurchased || isInCart) ? "" : $"AddToCart({book.BookID})")">
                                    @* <svg viewBox="0 0 24 24" style="width: 20px; height: 20px; stroke: currentColor; fill: none;">
                                        <circle cx="9" cy="21" r="1"></circle>
                                        <circle cx="20" cy="21" r="1"></circle>
                                        <path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"></path>
                                    </svg> *@
                                    @(isPurchased
                                                            ? "Purchased ✔"
                                                            : isInCart
                                                            ? "In cart 🛒"
                                                            : "Add to Cart")
                                    </button>
                            </div>
                        </div>
                    }
                    else
                    {
                        string path = $"{@Url.Action("ViewBookPreview", "Registered", new { bookID = book.BookID })}";
                        <div class="book-card" data-title="@book.Book_name">
                            <a href=@path class="book-cover">
                                @if (!string.IsNullOrWhiteSpace(book.SubjectID))
                                {
                                    <span class="book-subject-tag">
                                        @book.SubjectID
                                    </span>
                                }
                                <img class="image_div" src="@Url.Action("GetBookImage", "Guest", new { bookID = book.BookID })">
                             </a>
                            <div class="book-info">
                                <h3>@book.Book_name</h3>
                                <p class="book-author">@book.Author_name</p>
                                <p class="book-price">$@book.Book_price</p>
                                <button class="add-to-cart error" id="book-@book.BookID" disabled onclick="AddToCart(@book.BookID)">
                                    @* <svg viewBox="0 0 24 24" style="width: 20px; height: 20px; stroke: currentColor; fill: none;">
                                    <circle cx="9" cy="21" r="1"></circle>
                                    <circle cx="20" cy="21" r="1"></circle>
                                    <path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"></path>
                                </svg> *@
                                    Out of stock
                                </button>
                            </div>
                        </div>
                    }
                }
            }



        </div>






    </div>
</main>

<script>
    
    async function AddToCart(bookId) {
        try {
            const response = await fetch(
                `@(Url.Action("AddToCart", "Registered"))?bookID=${encodeURIComponent(bookId)}`,
                {
                    method: 'GET',
                    headers: {
                        'Accept': 'application/json'
                    }
                }
            );

            // alert(`https://localhost:7121/Registered/AddToCart?bookID=${encodeURIComponent(bookId)}`);
            if (!response.ok) {
                alert("Didn't work");
                // showAddToCartError(bookId);
                return;
            }

            const data = await response.json(); // { success: true/false }
            // alert(data.success);


            if (data.success) {
                showAddToCartSuccess(bookId);
            }else {
                showAddToCartError(bookId);
            }

        } catch (err) {
            console.error(err);
            showAddToCartError(bookId);
        }
    }

    function showAddToCartSuccess(bookId) {
        const add_to_cart_button = document.getElementById('book-' + bookId);
        if (!add_to_cart_button) return;

        add_to_cart_button.textContent = 'Added ✔';
        add_to_cart_button.disabled = true;
        add_to_cart_button.classList.add('in-cart');
    }

    function showAddToCartError(bookId) {
        const btn = document.getElementById('book-' + bookId);
        if (!btn) return;

        btn.textContent = 'Failed';
        btn.classList.add('error');
        btn.disabled = true;


        /*<svg viewBox="0 0 24 24" style="width: 20px; height: 20px; stroke: currentColor; fill: none;">
                                <circle cx="9" cy="21" r="1"></circle>
                                <circle cx="20" cy="21" r="1"></circle>
                                <path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"></path>
                            </svg>*/
          setTimeout(() => {
        btn.textContent = `Add to Cart;`;
        btn.classList.remove('error');
        btn.disabled = false;
        }, 2000);
    }

    

     function searchBooks() {
        const searchTerm = document.getElementById('search-input').value.toLowerCase();
        const bookCards = document.querySelectorAll('.book-card');
        bookCards.forEach(card => {
            const title = card.dataset.title.toLowerCase();
            const author = card.querySelector(".book-author").textContent.toLowerCase();;
            if (title.includes(searchTerm) || author.includes(searchTerm)) {
                card.style.display = 'block';
            } else {
                card.style.display = 'none';
            }
        });
    }

        function openFiltersSidebar() {
        const sidebar = document.getElementById("filters-sidebar");
        const overlay = document.getElementById("filters-overlay");

        sidebar.classList.add("open");
        overlay.classList.add("visible");

        document.body.classList.add("filters-sidebar-open");
    }


    function closeFiltersSidebar() {
        const sidebar = document.getElementById("filters-sidebar");
        const overlay = document.getElementById("filters-overlay");

        sidebar.classList.remove("open");
        overlay.classList.remove("visible");

        document.body.classList.remove("filters-sidebar-open");
    }


    document.addEventListener("keydown", function (event) {
        if (event.key === "Escape") {
            closeFiltersSidebar();
        }
    });
</script>





<!-- Book Cards -->
@* @if (Model != null && Model.Any())
            {
                @foreach (Book book in Model)
                {
                    <div class="book-card">
                            <span class="category-tag">Fiction</span>
                        <div class="book-image">
                            <img class="image_div" src="../Images/HomePage_Guest_Images/0b9b4d7903c0f8b8cdd4d451e7ae3d19.jpg">
                        </div>
                        <h3 class="book-title">@book.Book_name</h3>
                        <div class="book-actions">
                            <button class="action-btn btn-add-cart">Add to Cart</button>
                            <button class="action-btn btn-preview">Preview</button>
                        </div>
                    </div>
                }
            } *@



@*  <script>
     function AddToCart(let id){
        const path = 'https://localhost:7121/Registered/ViewBookPreview?bookID=${id}';
        fetch(path)
            .then(response => {
                if (!response.ok) {
                    throw new Error(`Network response was not ok: ${response.status}`);
                }
                return response.json();
            })
            .then(data => {
                console.log(data);
            })
            .catch(error => {
                console.error('Fetch error:', error);
            });

     }
 </script> *@