<!--<Window x:Class="IIStudyDESKTOP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IIStudyDESKTOP"
        mc:Ignorable="d"
        Title="MainWindow" Height="1080" Width="1920"
        ResizeMode="NoResize"
        >
    <Window.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="30"></Setter>
            <Setter Property="Margin" Value="20"></Setter>
            <Setter Property="Height" Value="40"></Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="159*"/>
            <ColumnDefinition Width="156*"/>
            <ColumnDefinition Width="426*"/>
            <ColumnDefinition Width="24*"/>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="1145*"/>
        </Grid.ColumnDefinitions>
        --><!--<Grid.ColumnDefinitions>
            <ColumnDefinition Width="191*"/>
            <ColumnDefinition Width="9*"/>
        </Grid.ColumnDefinitions>--><!--
        <TextBlock Width="365"   Background="#FFB7B2B2" Margin="0,0,376,0" Grid.ColumnSpan="3" >
            <Hyperlink   Style="{StaticResource ButtonStyle}" Click="Button_Click" >HomePage</Hyperlink>
            <Hyperlink  Style="{StaticResource ButtonStyle}" Click="Button_Click" >Books</Hyperlink>
            <Hyperlink  Style="{StaticResource ButtonStyle}" Click="Button_Click" >Exams</Hyperlink>
            <Hyperlink  Style="{StaticResource ButtonStyle}" Click="Button_Click" >registers</Hyperlink>
            <Hyperlink  Style="{StaticResource ButtonStyle}" Click="Button_Click" >Orders</Hyperlink> 
            <Hyperlink  Style="{StaticResource ButtonStyle}" Click="Button_Click" >Calendar</Hyperlink>
        </TextBlock>


    </Grid>

</Window>-->
<Window x:Class="IIStudyDESKTOP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IIStudyDESKTOP"
        mc:Ignorable="d"
        Title="MainWindow" Height="1080" Width="1920"
        
        WindowStartupLocation="CenterScreen"
        Background="#FF667EEA">

    <Window.Resources>
        <!-- Gradient Background Brush -->
        <LinearGradientBrush x:Key="GradientBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#667eea" Offset="0"/>
            <GradientStop Color="#764ba2" Offset="1"/>
        </LinearGradientBrush>

        <!-- Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#667eea" Offset="0"/>
                        <GradientStop Color="#764ba2" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform Y="-2"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#667eea" 
                                                        BlurRadius="20" 
                                                        ShadowDepth="8" 
                                                        Opacity="0.4"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#667eea"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="BorderBrush" Value="#e2e8f0"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#667eea"/>
                                <Setter Property="Background" Value="#f7fafc"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#e2e8f0"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#667eea"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#e2e8f0"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- Label Style -->
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#4a5568"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>

    <DockPanel LastChildFill="True">
        <!-- Main Container -->
        
        <!-- Header -->
        <Border  DockPanel.Dock="Left"  
                Background="White" 
                Padding="30,20"
                CornerRadius="0">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
            </Border.Effect>

            <Grid>
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <!-- Logo -->
                <TextBlock Grid.Row="0" 
                          FontSize="24" 
                          FontWeight="ExtraBold"
                          VerticalAlignment="Center">
                    <TextBlock.Foreground>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#667eea" Offset="0"/>
                            <GradientStop Color="#764ba2" Offset="1"/>
                        </LinearGradientBrush>
                    </TextBlock.Foreground>
                    📚IIStudy
                </TextBlock>

                <!-- Navigation Buttons -->
                <StackPanel Grid.Row="2" 
                           Orientation="Vertical" 
                           HorizontalAlignment="Right">
                    <Button Content="Home" Style="{StaticResource SecondaryButton}" Padding="60,10" Margin="0, 10 ,0 , 10"/>
                    <Button Content="Books" Style="{StaticResource SecondaryButton}" Padding="16,10" Margin="0, 10 ,0 , 10"/>
                    <Button Content="Exams" Style="{StaticResource SecondaryButton}" Padding="16,10" Margin="0, 10 ,0 , 10"/>
                    <Button Content="Orders" Style="{StaticResource SecondaryButton}" Padding="16,10" Margin="0, 10 ,0 , 10"/>
                    <Button Content="Users" Style="{StaticResource SecondaryButton}" Padding="16,10" Margin="0, 10 ,0 , 10"/>

                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <ScrollViewer DockPanel.Dock="Right" 
                      Grid.Row="0"
                      VerticalAlignment="Stretch"
                     VerticalScrollBarVisibility="Auto"
                     Margin="40,40,40,40">
            <Border Background="White" 
                    CornerRadius="24" 
                    Padding="50">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="0" Opacity="0.15"/>
                </Border.Effect>

                <StackPanel>
                    <!-- Page Title -->
                    <Grid Margin="0,0,0,40">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" 
                                Width="6" 
                                Height="40" 
                                CornerRadius="3"
                                Margin="0,0,12,0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#667eea" Offset="0"/>
                                    <GradientStop Color="#764ba2" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>

                        <TextBlock Grid.Column="1" 
                                  Text="Update Book Information" 
                                  FontSize="32" 
                                  FontWeight="ExtraBold"
                                  Foreground="#2d3748"
                                  VerticalAlignment="Center"/>
                    </Grid>

                    <!-- Form Grid -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Book ID -->
                        <StackPanel Grid.Row="0" Grid.Column="0">
                            <TextBlock Text="Book ID" Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="txtBookID" 
                                    Style="{StaticResource ModernTextBox}"
                                    IsReadOnly="True"
                                    Text="BK-12345"
                                    Background="#f7fafc"/>
                        </StackPanel>

                        <!-- Book Name -->
                        <StackPanel Grid.Row="0" Grid.Column="2">
                            <TextBlock Text="Book Name *" Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="txtBookName" 
                                    Style="{StaticResource ModernTextBox}"
                                    Text="To Kill a Mockingbird"/>
                        </StackPanel>

                        <!-- Author Name -->
                        <StackPanel Grid.Row="2" Grid.Column="0">
                            <TextBlock Text="Author Name *" Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="txtAuthorName" 
                                    Style="{StaticResource ModernTextBox}"
                                    Text="Harper Lee"/>
                        </StackPanel>

                        <!-- Price -->
                        <StackPanel Grid.Row="2" Grid.Column="2">
                            <TextBlock Text="Price ($) *" Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="txtPrice" 
                                    Style="{StaticResource ModernTextBox}"
                                    Text="24.99"/>
                        </StackPanel>

                        <!-- Subject -->
                        <StackPanel Grid.Row="4" Grid.Column="0">
                            <TextBlock Text="Subject *" Style="{StaticResource FormLabel}"/>
                            <ComboBox x:Name="cmbSubject" 
                                     Style="{StaticResource ModernComboBox}"
                                     SelectedIndex="0">
                                <ComboBoxItem Content="Fiction"/>
                                <ComboBoxItem Content="Science"/>
                                <ComboBoxItem Content="History"/>
                                <ComboBoxItem Content="Technology"/>
                                <ComboBoxItem Content="Biography"/>
                                <ComboBoxItem Content="Philosophy"/>
                            </ComboBox>
                        </StackPanel>

                        <!-- Pages -->
                        <StackPanel Grid.Row="4" Grid.Column="2">
                            <TextBlock Text="Number of Pages" Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="txtPages" 
                                    Style="{StaticResource ModernTextBox}"
                                    Text="336"/>
                        </StackPanel>

                        <!-- ISBN -->
                        <StackPanel Grid.Row="6" Grid.Column="0">
                            <TextBlock Text="ISBN" Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="txtISBN" 
                                    Style="{StaticResource ModernTextBox}"
                                    Text="978-0-06-112008-4"/>
                        </StackPanel>

                        <!-- Publication Year -->
                        <StackPanel Grid.Row="6" Grid.Column="2">
                            <TextBlock Text="Publication Year" Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="txtPublicationYear" 
                                    Style="{StaticResource ModernTextBox}"
                                    Text="1960"/>
                        </StackPanel>

                        <!-- In Stock -->
                        <StackPanel Grid.Row="8" Grid.Column="0">
                            <TextBlock Text="Availability" Style="{StaticResource FormLabel}"/>
                            <CheckBox x:Name="chkInStock" 
                                     Content="In Stock" 
                                     IsChecked="True"
                                     FontSize="14"
                                     FontWeight="SemiBold"
                                     Foreground="#4a5568"
                                     Margin="0,8,0,0"/>
                        </StackPanel>

                        <!-- Format -->
                        <StackPanel Grid.Row="8" Grid.Column="2">
                            <TextBlock Text="Format" Style="{StaticResource FormLabel}"/>
                            <ComboBox x:Name="cmbFormat" 
                                     Style="{StaticResource ModernComboBox}"
                                     SelectedIndex="0">
                                <ComboBoxItem Content="PDF"/>
                                <ComboBoxItem Content="EPUB"/>
                                <ComboBoxItem Content="Hardcover"/>
                                <ComboBoxItem Content="Paperback"/>
                            </ComboBox>
                        </StackPanel>

                        <!-- Description -->
                        <StackPanel Grid.Row="10" Grid.Column="0" Grid.ColumnSpan="3">
                            <TextBlock Text="Description" Style="{StaticResource FormLabel}"/>
                            <TextBox x:Name="txtDescription" 
                                    Style="{StaticResource ModernTextBox}"
                                    TextWrapping="Wrap"
                                    AcceptsReturn="True"
                                    Height="120"
                                    VerticalScrollBarVisibility="Auto"
                                    Text="A gripping, heart-wrenching, and wholly remarkable tale of coming-of-age in a South poisoned by virulent prejudice."/>
                        </StackPanel>

                        <!-- Action Buttons -->
                        <StackPanel Grid.Row="12" Grid.Column="0" Grid.ColumnSpan="3" 
                                   Orientation="Horizontal" 
                                   HorizontalAlignment="Right"
                                   >
                            <Button Content="Cancel" 
                                   Style="{StaticResource SecondaryButton}"
                                   Width="140"
                                   /> <!--Click="CancelButton_Click"-->
                            <Button Content="Update Book" 
                                   Style="{StaticResource ModernButton}"
                                   Width="160"
                                   /> <!--Click="UpdateButton_Click"-->
                        </StackPanel>
                    </Grid>

                    <!-- Info Message -->
                    <Border Background="#eef2ff" 
                           BorderBrush="#c7d2fe" 
                           BorderThickness="2"
                           CornerRadius="12"
                           Padding="16"
                           Margin="0,30,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="ℹ️" 
                                      FontSize="20" 
                                      Margin="0,0,12,0"
                                      VerticalAlignment="Center"/>
                            <TextBlock TextWrapping="Wrap"
                                      Foreground="#4338ca"
                                      FontSize="13">
                                <Run FontWeight="SemiBold">Note:</Run>
                                <Run>Fields marked with * are required. Make sure all information is accurate before updating.</Run>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </DockPanel>
</Window>